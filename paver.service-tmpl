[Unit]
Description=Paver service
Requires=nginx.service
Requires=postgresql.service

[Service]
User=${USER}
WorkingDirectory=${WORKSPACE}
ExecStartPre=rm -f ${SOCKET}
ExecStart=${CMD}

[Install]
WantedBy=multi-user.target
